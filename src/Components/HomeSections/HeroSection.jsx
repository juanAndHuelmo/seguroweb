import QuotationForm from '../Forms/QuotationForm';

function HeroSection({ openQuote }) {
  return (
    <section className="hero-section">
      <QuotationForm />
    </section>
  );
}

export default HeroSection;